<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>book stuff</title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <div class="user-books">
    <h1><%= user.username %>'s Books</h1>

    <% if (books && books.length > 0) { %>
        <ul>
            <% books.forEach(book => { %>
                <li>
                    <a href="/users/<%=user.id%>/books/community/<%=user.id%>/<%=book.id%>"><%= book.title %></a>
                </li> 
            <% }) %>
        </ul>
    <% } else { %>
        <p>No Books </p>
    <% } %>
    <a href="/">Back to My Books</a>
</div>
  </body>
</html>